#include<stdio.h>
int main()
{
int v,x,pair,i;
printf("enter the child");
scanf("%d",&v);
printf("pairs");
scanf("%d",&pair);
for(i=0;i<2*v;i++)
{
x=2*v-i;
if(pair!=x)
{
printf("The pair is%d for days %d is %d\n",p,j++,p);
}
}
return 0;
}